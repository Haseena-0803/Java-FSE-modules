# Bootstrap 5 Custom Sass Setup Guide

---

## Step 1: Initialize npm project and install dependencies

```bash
mkdir bootstrap-sass-project
cd bootstrap-sass-project
npm init -y
npm install bootstrap sass
